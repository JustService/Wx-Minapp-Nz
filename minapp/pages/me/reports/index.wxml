<view class="container">
  <block wx:for="{{reports}}" wx:key="id">
    <view class="card report-card" bindtap="goReport" data-id="{{item.id}}">
      <view class="title">{{item.title}}</view>
      <text class="hint">{{item.date}}</text>
    </view>
  </block>
</view>
